<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="userpageMapper">

	<select id="SearchUser" parameterType="String" resultMap="memberResultSet">
 		SELECT 가져올 컬럼들
		FROM MEMBER
		WHERE NICKNAME = #{nickname}
		AND STATUS = 'Y'
 	</select>
 	
 	<select id="SearchUserDiet" parameterType="String" resultMap="memberResultSet">
 		SELECT 가져올 컬럼들
		FROM MEMBER
		WHERE NICKNAME = #{nickname}
		AND STATUS = 'Y'
 	</select>
 	
 	<select id="SearchUserExercise" parameterType="String" resultMap="memberResultSet">
 		SELECT 가져올 컬럼들
		FROM MEMBER
		WHERE NICKNAME = #{nickname}
		AND STATUS = 'Y'
 	</select>
  
  
</mapper>